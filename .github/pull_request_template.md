## What this changes

<!-- Which index, and why: a new source, a removed page, a version or branch correction... -->

## Checklist

- [ ] I changed `build_indexes()` in `scripts/generate.py`, not an `llms.txt` file by hand.
- [ ] I ran `python scripts/generate.py` and committed the regenerated indexes with the change.
- [ ] `python scripts/validate.py` passes.
- [ ] New sources are the **released** documentation, not a development branch.
- [ ] Every new citation URL was confirmed to show the same content as the fetched file.

## Generator report

<!-- Paste the generator's summary and any "skipped" or "no public page found" lines. -->
